# Dev Notes

## Screenshots / Playwright

Do not save screenshots into the project root. `*.png` is gitignored but clutters the working tree. Save to a temp directory (e.g. `/tmp/`) or discard after inspection.
